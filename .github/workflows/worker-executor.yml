name: Worker patch executor (disabled)

# Patch transport through PR comments is intentionally disabled.
# Workers persist executable state in branch commits and the PR checklist only.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR-comment patch execution is disabled; workers must push branch commits directly."
