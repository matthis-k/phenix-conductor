# PR 506 working state

## Current state

Base: #504 compacted active head (`af670e28`)
Phase: dependency-blocked implementation

## Decisions

- #506 remains a direct child of #504 and is restacked on the current compacted #504 head.
- #503/#505 ABI and client-tool behavior are merged dependencies.
- `spec/nvim-integrations-implementation.md` fixes the public modules, adapter registry, editor types, form model, default tool schemas/bounds, privileged-tool policy, lifetime behavior, fallback table, tests, and implementation order.
- `spec/nvim-integrations-value-capability.md` fixes which Lua functions remain frontend-local and which cross the runtime as #503/#505 callable values.
- No architectural decision remains intentionally deferred in #506.

## Findings

- #504 owns the configured `phenix-acp` application, production session observable, interaction handlers, and runtime review state required by #506.
- #506 implementation must not invent substitutes for those surfaces.
- The child branch contains only #506-owned integration specifications and this handoff on top of #504.

## Next

- Finish/merge #504 to its green boundary.
- Restack #506 only if #504 moves again.
- Implement `phenix_nvim.integrations`, `phenix_nvim.forms`, and `phenix_nvim.tools` exactly from the implementation spec.
- Remove this temporary file before #506 merge.

## Stack

Parent: #504 (`spec/nvim-client-package`) at `af670e28`.
Children: none currently identified.
