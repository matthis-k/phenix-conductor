# PR 514 working state

## Current state

Head: `fa244bae3e547f9ff9874a1081cc473ae0d4f00f` (green). Base: `main`. Phase: implementation.

## Decisions

- Preserve every existing error message exactly; only errors whose formatting maps directly to `thiserror` attributes are migrated in this tranche.
- Leave errors that assemble collections or paths dynamically for a later, deliberate pass.

## Findings

- Exact head `52554f5f126bd6d308930b4fb1181e0686eb30b0` is green after correcting `PersistenceError`'s `rusqlite` conversion attribute.
- Local Rust tooling is unavailable; rely on exact-head CI after diff and formatting checks.
- CI #10627 and Maintenance #5492 passed the core error-mechanics tranche.

## Next

- Push the remaining directly-mappable domain delegation errors, wait for exact-head CI, then continue the audit from validated IDs/invariants.

## Stack

Parent: `main`.
Children: none identified.
