# PR 503 working state

## Current state

Head: e77ebf09d4dc19649a438d23799b74b9b9b5d780
Base: main
Phase: repair

## Decisions

- Observable resources stay ordinary runtime-owned `get` and `listen` callables. The listener is a generic client-owned `CallableRef`.
- The listener input uses the core canonical delivery schema. It preserves commit, value, version, address, subscription, generation, diff, and full-snapshot data.

## Findings

- CI 10328 on 2afaa484: Maintenance passed and committed e77ebf09 with rustfmt. Rust unit tests expose a pre-existing missing `listen.clone()` in the nested observable test. Clippy rejects the new test's unavailable `AnyUserData::call_method` helper.
- `listen` supports a relative path and typed listener schema.
- The PR checklist is stale. Reconcile it only after the remaining observable projection work and validation complete.

## Next

- Push the grouped formatting, test-API, and core-test repair. Then wait for exact-head CI.
- If green, add the missing stale queued subscription regression and reconcile the PR checklist before final validation.

## Stack

- Parent: main
- Children: #505 -> #504 -> #506
- Rebase implications: restack children after #503 merges.
