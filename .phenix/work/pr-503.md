# PR 503 working state

## Current state

Head: 95ca294f50ec97b691fed18d97365c8cac9b6583
Base: main
Phase: implementation

## Decisions

- Observable resources stay ordinary runtime-owned `get` and `listen` callables. The listener is a generic client-owned `CallableRef`.
- The listener input uses the core canonical delivery schema. It preserves commit, value, version, address, subscription, generation, diff, and full-snapshot data.

## Findings

- Exact-head CI and Maintenance for 95ca294f require manual approval. This does not cover later work.
- `listen` supports a relative path and typed listener schema.
- The PR checklist is stale. Reconcile it only after the remaining observable projection work and validation complete.

## Next

- Native lazy Lua delivery projection now retains the canonical value until a listener calls an accessor. The public helpers support `resource:get()` and `resource:listen(opts, listener)` without a second protocol.
- Run formatter and focused Lua/Core tests in a toolchain-enabled environment. Then add the missing stale queued subscription regression and reconcile the PR checklist before one push.

## Stack

- Parent: main
- Children: #505 -> #504 -> #506
- Rebase implications: restack children after #503 merges.
