# PR 505 working state

## Current state

Head: current branch HEAD after this handoff-only commit
Base: main (67c46f29)
Phase: validation blocked on manual CI approval

## Decisions

- #503 is on main. Reuse its generic CallableRef and dispatcher rather than callback-specific transport.
- Admissions are ephemeral and generation scoped; the backend sees only ordinary CallableDescriptors through ToolProvision.
- One ACP callback queue serves one client connection, so its add/invocation paths accept only that owner and generation.
- Provider adapters translate the backend-neutral model tool surface at the protocol boundary; providers do not gain a client-tool-specific path.
- ToolProvision merges ordinary CallableDescriptors without accepting ClientToolAdmissions. Admission ownership stays above the backend boundary.

## Findings

- This branch is based directly on main after #503.
- Transport closure permanently retires the client owner generation. New service instances start with an empty admission registry, so reconnect cannot revive prior tools.
- A disconnected generic client callable retires its capability references and session admissions. Application session ids are converted to the domain boundary before admission/removal.
- The ACP application service admits client tools and exposes callable discovery/invocation through the generic capability dispatcher; explicit removal disables both.
- Execution uses the public model-routing ABI. The model ABI carries backend-neutral tool descriptors and structured tool calls, and the agent-loop regression proves both survive its routing boundary.
- OpenAI Responses, OpenAI Chat Completions, and Anthropic Messages encode the same backend-neutral descriptor surface and decode structured provider tool calls into ModelToolCall.
- Exact-head CI 34476103854 for ed8eb5eb completed successfully, including Source, Rust workspace/unit/doc tests, Clippy, Product, integration/runtime checks, SDK, and Maintenance.
- This run replaced backend-specific ClientToolAdmissions merging with origin-neutral ToolProvision::merge_callables and added deterministic descriptor parity/duplicate coverage in e426857e.
- CI 34478036869 compiled the Rust workspace at e426857e. Source found only rustfmt drift in the new assertion while the remaining jobs were still running.
- Maintenance 34478037064 succeeded and pushed rustfmt-only commit ed0fb5097ebf8180dc1f59764110e918d17e9789.
- Required CI and Maintenance for ed0fb5097ebf8180dc1f59764110e918d17e9789 are action_required. This is now a real manual-CI boundary, unlike the stale handoff at the start of this run.
- No production BackendHost implementation currently joins admitted client callables to backend execution. That remains the main execution integration gap.

## Next

- Approve required CI for current HEAD and inspect every result before further implementation.
- Wire admitted descriptors into the application/session execution assembly before ToolProvision.
- Add the ordinary tool route so permission checks, input/output schema validation, and generic CallableRef dispatch share the runtime-tool lifecycle.
- Add Lua tools.register sugar only on the generic callable API after the execution route exists.
- Add packaged model -> client callable -> Lua -> ordinary tool-result coverage.

## Stack

- Parent: #503 merged into main at 67c46f29.
- Children: #504, then #506.
- Rebase implications: restack children after material #505 changes, before #505 is considered complete.
