# PR 505 working state

## Current state

Head: 213fdf41
Base: main (23342b8)
Phase: implementation

## Decisions

- #503 is integrated at the base; reuse its generic `CallableRef` and capability dispatcher rather than reviving callback-specific transport.
- Keep admissions as an ephemeral, owner-generation-scoped registry; the backend sees only ordinary `CallableDescriptor`s through `ToolProvision`.
- Merge client admissions into `ToolProvision` before backend preparation, reject duplicate ids deterministically, and order the final surface by callable id.

## Findings

- The application interface supplies fixed add/remove payloads; `invoke` is represented as `PhenixValue` and must be checked as a callable against sibling schemas at admission.
- `phenix-binding-lua` already lifts host functions into client-owned `CallableRef`s and dispatches them on its polling thread.
- Exact-head CI 10388 and Maintenance 5293 passed at 213fdf41 after the maintenance autofix.
- `ToolProvision::with_client_admissions` projects session admissions without a backend-specific client-tool concept. The application operations, session integration, ordinary execution route, connection cleanup, and Lua sugar remain incomplete.

## Next

- Implement application add/remove operations with connection-local admission ownership and a focused regression. Then use the same registry when constructing a session's backend tool provision.
- Route an admitted callable through the generic dispatcher with normal permission and schema validation before adding Lua registration sugar.

## Stack

- Parent: #503 merged into `feat/generated-lua-binding` at 23342b8.
- Children: #504, then #506.
- Rebase implications: restack children after material #505 implementation changes.
