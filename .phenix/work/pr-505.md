# PR 505 working state

## Current state

Head: 854b0dfcbd497f4417f5e086ae3ff38689e2957a (server re-entry repair follows)
Base: main (67c46f29)
Phase: repair

## Decisions

- Reuse #503 generic `CallableRef` dispatch. No client-tool-specific callback protocol.
- Admissions are ephemeral and client-generation scoped. Backends see ordinary `CallableDescriptor` and `ToolProvision` values.
- The application host owns admitted-client execution: permission first, then generic callable dispatch.
- ACP handlers must keep the connection event loop free while work can perform nested ACP traffic. Both directions of the callback bridge use `connection.spawn(...)` before awaiting nested work.
- Observable delivery stays synchronous at the core SDK boundary. `SharedCapabilityRegistry` releases its lock before provider code, and the application service already executes blocking SDK work outside the Tokio worker.

## Findings

- #503 is on main. #505 is based directly on main.
- Generic capability dispatch validates callable contract/input before provider code and output before release. Cancellation and disconnect stay structural.
- ACP application operations admit, list, invoke, and remove client tools through the generic capability registry.
- Explicit removal unregisters a lifted callable after its final admission disappears. Disconnect retires the whole client generation.
- `execute_admitted_client_tool_call` resolves a model call against session admission, runs ordinary permission first, preserves call identity, dispatches through `InvokeCallable`, and emits existing tool result/failure changes. Unit coverage includes allow, deny, and permission-free paths.
- The execution/model path carries backend-neutral tool descriptors and typed `ModelToolCall`s, but the agent loop still returns tool calls without executing them.
- `ToolProvision::merge_callables` rejects duplicate ids, but production session/model assembly does not yet merge admitted client descriptors with runtime/plugin descriptors before provisioning.
- Exact-head `854b0dfc` passes Maintenance and every CI lane except Product. Rust unit tests are green after restoring synchronous observable delivery. Product still times out waiting for the initial Lua observable callback.
- Client-side callback spawning alone is insufficient. The server extension request handler awaits `ApplicationAdapter::extension_request` inline, while `InvokeCapability` can synchronously invoke a client-owned callback. That keeps the ACP dispatch handler occupied during the nested server-to-client request.
- The ACP SDK pattern already used by this repository is to move nested request work into `connection.spawn(...)`. Apply the same rule to the server extension request handler, then validate the packaged Lua fixture.
- The packaged Lua fixture covers SDK acquisition, observable callback delivery, client-tool registration, invocation, removal, and stale invocation through ACP. It does not yet prove the production model-loop join.

## Tried / rejected

- Per-subscription observable delivery thread: rejected. Product still timed out and synchronous core semantics regressed.
- Poll-count and wall-clock waits in the Lua fixture: rejected. The missing callback is protocol re-entry, not host polling cadence.
- Client-side callback-handler spawn alone: insufficient. It fixed one side of re-entry but Product still timed out.
- Client-specific backend tool protocol: rejected. Use the ordinary model tool and generic capability contracts.
- Local Cargo/Nix validation is unavailable in this worker environment. Exact-head CI is the compile, format, and packaged-runtime authority.

## Next

- Validate the server extension-handler spawn together with the existing client callback-handler spawn at exact head.
- If Product is green, finish the production model-loop join: merge admitted descriptors into model provisioning, execute returned admitted calls through the ordinary lifecycle, and add end-to-end coverage.
- Then reconcile the PR checklist, remove this file, validate the cleaned head, and restack children.

## Stack

Parent: #503 merged into main at 67c46f29.
Children: #504 -> #506.
Rebase implications: material #505 changes require restacking #504 and #506 before #505 is complete. Current child heads must be re-read before restacking.
