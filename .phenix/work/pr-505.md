# PR 505 working state

## Current state

Head: 5eaaa951
Base: main (67c46f29)
Phase: validation

## Decisions

- #503 is on main. Reuse its generic CallableRef and dispatcher rather than callback-specific transport.
- Admissions are ephemeral and generation scoped; the backend sees ordinary CallableDescriptors through ToolProvision.
- One ACP callback queue serves one client connection, so its add/invocation paths accept only that owner and generation.
- Provider adapters translate the backend-neutral model tool surface at the protocol boundary; providers do not gain a client-tool-specific path.

## Findings

- This branch was restacked directly on main in cd79e5bf; 80184231 is the maintenance autofix on top.
- Exact-head CI 34443375392 and Maintenance 34443375377 passed on f5db28c5.
- Exact-head CI 34457895425 compiled and passed every test, product, runtime, and integration job. Source failed only because Rust formatting changed the local slice. The exact formatter diff is queued for repair.
- Exact-head CI 34460554584 passed after the formatter repair.
- Transport closure permanently retires the client owner generation. New service instances start with an empty admission registry, so reconnect cannot revive prior tools.
- ToolProvision exposes a merge helper, but no conductor execution path constructs it yet.
- A disconnected generic client callable now retires its capability references and session admissions. Application session ids are converted to the domain boundary before admission/removal.
- The ACP application service admitted client tools but did not expose ordinary callable discovery/invocation. The local slice adds both through the generic capability dispatcher and proves explicit removal disables both.
- Execution now uses the public model-routing ABI. The private duplicate request/response types are gone.
- The model ABI now carries backend-neutral tool descriptors and structured tool calls. The agent-loop regression proves both survive its routing boundary.
- OpenAI Responses, OpenAI Chat Completions, and Anthropic Messages now encode the same backend-neutral descriptor surface and decode structured provider tool calls into ModelToolCall. Protocol regressions cover all three.
- Exact-head CI 34466281846 exposed only a missing import and an invalid `Eq` derive in the typed-envelope slice. Maintenance passed.
- Exact-head CI 34467424467 passed every substantive check except Clippy/unit tests, both blocked by the same obsolete private routing-service constant. The repair removes it.
- Exact-head CI 34468467163 then passed every job except a fixture-only refutable-pattern compile error. The fixture now rejects non-inference model commands explicitly.

## Next

- Validate the provider protocol slice at exact HEAD; repair formatting/compile issues as one batch if CI finds any.
- Wire admitted descriptors into session provisioning and ordinary execution.
- Add internal tool routes so permission checks, schema validation, and generic CallableRef dispatch share the runtime-tool lifecycle.
- Add Lua `tools.register` only on the generic callable API after the execution route exists.

## Stack

- Parent: #503 merged into main at 67c46f29.
- Children: #504, then #506.
- Rebase implications: restack children after material #505 changes.
