[package]
name = "phenix-adapter-acp"
version = "0.1.0"
edition = "2021"
publish = false

[package.metadata.phenix]
role = "runtime-plugin"

[lib]
path = "src/lib.rs"

[dependencies]
agent-client-protocol.workspace = true
phenix-core = { path = "../phenix-core" }
phenix-sdk = { path = "../phenix-sdk" }
