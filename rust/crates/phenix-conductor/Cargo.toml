[package]
name = "phenix-conductor"
version = "0.1.0"
edition = "2021"
publish = false

[package.metadata.phenix]
role = "application"

[dependencies]
phenix-core = { path = "../phenix-core" }
phenix-client = { path = "../phenix-client" }
serde_json.workspace = true
