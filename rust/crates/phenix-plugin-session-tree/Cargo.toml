[package]
name = "phenix-plugin-session-tree"
version = "0.1.0"
edition = "2021"
publish = false

[package.metadata.phenix]
role = "runtime-plugin"

[package.metadata.phenix.implementation-dependencies]
phenix-plugin-sessions = "Session-tree child creation consumes the flat-session mutation transaction interface."

[dependencies]
phenix-core = { path = "../phenix-core" }
phenix-sdk = { path = "../phenix-sdk" }
phenix-sdk-macros = { path = "../phenix-sdk-macros" }
phenix-plugin-sessions = { path = "../phenix-plugin-sessions" }
serde.workspace = true
serde_json.workspace = true
